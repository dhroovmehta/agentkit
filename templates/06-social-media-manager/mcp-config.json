{
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "/path/to/your/content-folder"],
      "description": "Access content calendar files, brand guidelines, and previous posts."
    },
    "brave-search": {
      "command": "npx",
      "args": ["-y", "@anthropic/mcp-server-brave-search"],
      "env": {"BRAVE_API_KEY": "YOUR_BRAVE_API_KEY"},
      "description": "Research trending topics, hashtags, and competitor social activity."
    },
    "fetch": {
      "command": "npx",
      "args": ["-y", "@anthropic/mcp-server-fetch"],
      "description": "Fetch trending content and social media insights."
    }
  }
}
